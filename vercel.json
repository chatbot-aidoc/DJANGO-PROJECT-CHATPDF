{
    "builds": [{
        "src": "user_management/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "500mb", "runtime": "python3.10" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "user_management/wsgi.py"
        }
    ]
}
